{
  "name": "beatnation-groove-scribe",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/generate-runtime-globals.mjs && vite build && node scripts/write-asset-manifest.mjs",
    "build:deploy": "npm run build && node scripts/inject-html-bundles.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@rollup/plugin-inject": "^5.0.5",
    "vite": "^6.2.0",
    "vue": "^3.5.13"
  }
}
